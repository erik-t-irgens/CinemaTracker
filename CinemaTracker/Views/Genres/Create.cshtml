@{ Layout = "_Layout"; }


@model CinemaTracker.Models.Genre;

@using (Html.BeginForm())
{

    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)

    <input type = "Submit" value="Create Genre"/>
}